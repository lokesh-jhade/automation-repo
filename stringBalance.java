/*** 
Balanced Parentheses Problem

Given a string containing parentheses, determine if the string is balanced. A string is considered balanced if every opening parenthesis has a corresponding closing parenthesis of the same type, and vice versa. The parentheses must be properly nested.

Example:

- Input: s = "((()))" Output: true
- Input: s = "(()" Output: false
- Input: s = "{[()]}" Output: true
- Input: s = "{[(]}" Output: false ***/